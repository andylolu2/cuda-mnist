{
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "cute_test",
            "command": "make -C ${workspaceFolder}/cuda/build dbg=1 cute_test",
            "problemMatcher": [
                "$nvcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}